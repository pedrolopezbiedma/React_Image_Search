const SearchBar = ({ handleSearch }) => {
    return (
        <div>
            <input />
            <button onClick={handleSearch}>Click me!</button>
        </div>
    )
}

export default SearchBar